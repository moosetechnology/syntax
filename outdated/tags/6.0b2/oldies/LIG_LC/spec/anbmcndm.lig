*
* This is a LIG for the language {a^n b^m c^n d^m | n, m >= 0}
* Modelise les dependances croisees
*

<S>(..)		= <S>(..d) d	 	;
<S>(..)		= <T>(..)		;
*
<T>(..)		= a <T>(..) c		;
<T>(..)		= <B>(..)		;
*
<B>(..d)	= b <B>(..)		;
<B>()		= 			;

